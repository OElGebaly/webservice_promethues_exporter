apiVersion: v1
kind: Pod
metadata:
  name: webapp
  namespace: monitoring
  labels:
    app: webapp
spec:
  containers:
    - name: webapp
      image: idtest_webapp
      ports:
        - containerPort: 5000
      imagePullPolicy: IfNotPresent

    - name: requests-generator
      image: idtest_generator
      imagePullPolicy: IfNotPresent
